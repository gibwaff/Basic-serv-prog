<?php
// Заголовок страницы (TITLE) сохраняется в переменной, которая затем выводится с помощью PHP
$page_title = "Беляков Игорь Романович 241-3210. Лабораторная работа № А-1. Архитектура Web";
$current_file = basename(__FILE__);
?>
<!DOCTYPE html>
<html lang="ru">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><?php echo $page_title; ?></title>
    <link rel="stylesheet" href="css/style.css">
</head>
<body>

    <!-- Фиксированная шапка (header) -->
    <header class="site-header">
        <div class="logo">Веб-разработка на PHP</div>
        <nav class="main-menu">
            <a href="<?php
            $name = 'Главная';
            $link = 'index.php';
            $current_page = ($current_file == $link);
            echo $link;
            ?>"<?php
            if ($current_page)
                echo ' class="selected_menu"';
            ?>><?php
            echo $name;
            ?></a>
            <a href="<?php
            $name = 'О языке PHP';
            $link = 'php_intro.php';
            $current_page = ($current_file == $link);
            echo $link;
            ?>"<?php
            if ($current_page)
                echo ' class="selected_menu"';
            ?>><?php
            echo $name;
            ?></a>
            <a href="<?php
            $name = 'Архитектура Web';
            $link = 'architecture.php';
            $current_page = ($current_file == $link);
            echo $link;
            ?>"<?php
            if ($current_page)
                echo ' class="selected_menu"';
            ?>><?php
            echo $name;
            ?></a>
        </nav>
    </header>

    <!-- Основной контент страницы -->
    <main class="container">
        <h1>Клиент-серверная архитектура веб-приложений</h1>

        <h2>Протокол HTTP и модель взаимодействия</h2>
        <p>
            В основе функционирования глобальной сети Интернет лежит архитектурная модель «клиент-сервер». В этой
            парадигме пользовательское приложение (веб-браузер, мобильный клиент или консольная утилита) выступает
            инициатором сетевого соединения, формируя формализованный HTTP-запрос к удаленному веб-серверу.
            Запрос включает в себя стартовую строку (метод передачи, URL ресурса, версию протокола), набор заголовков
            с метаинформацией о типе клиента и поддерживаемых форматах сжатия, а также опциональное тело сообщения
            с пользовательскими данными.
        </p>
        <p>
            Веб-сервер принимает входящее сетевое соединение, идентифицирует запрашиваемый файл и, если запрашивается
            скрипт с расширением .php, передает управление интерпретатору PHP (например, через модули FastCGI / PHP-FPM).
            Интерпретатор последовательно выполняет исходные инструкции, вычисляет математические выражения, обращается
            к внешним ресурсам и направляет стандартный вывод (STDOUT) обратно веб-серверу. Сервер упаковывает результат
            в HTTP-ответ с соответствующим статус-кодом (200 OK, 404 Not Found, 500 Internal Server Error) и передает
            его по протоколу TCP/IP обратно клиенту.
        </p>

        <!-- Галерея изображений -->
        <div class="gallery">
            <div class="gallery-item">
                <?php
                // Смена фото в зависимости от секунды
                $s = date('s');
                $os = $s % 2;
                if ($os === 0) {
                    $name = 'fotos/foto1.jpg';
                } else {
                    $name = 'fotos/foto2.jpg';
                }
                echo '<img src="' . $name . '" alt="Меняющаяся фотография">';
                ?>
                <div class="gallery-caption">Динамический компонент (секунда обращения к узлу: <?php echo $s; ?>)</div>
            </div>
            <div class="gallery-item">
                <img src="fotos/static_img.jpg" alt="Модель взаимодействия узлов сети">
                <div class="gallery-caption">Статическое фото: топология взаимодействия клиентов и серверов</div>
            </div>
        </div>

        <h2>Роль веб-сервера и среды интерпретации PHP</h2>
        <p>
            Связка веб-сервера (Nginx / Apache) и интерпретатора PHP образует надежный фундамент для построения
            корпоративных информационных систем. Nginx берет на себя задачи балансировки сетевой нагрузки, терминации
            SSL/TLS-сертификатов, кеширования и мгновенной отдачи статических ресурсов (CSS-таблиц, изображений, шрифтов),
            в то время как вычислительно емкие задачи динамического рендеринга страниц передаются изолированным рабочим
            процессам PHP. Такой подход гарантирует отказоустойчивость и безопасность при высоких сетевых нагрузках.
        </p>

        <!-- Таблица (строка 1 полностью средствами PHP, строка 2 - ячейки динамические без переменных) -->
        <table class="content-table">
            <?php
            // HTML-код первой строки таблицы (включая теги <tr>...</tr>) полностью выводится средствами PHP
            echo '<tr><th>Уровень архитектуры</th><th>Протокол взаимодействия</th><th>Основная функциональная роль</th></tr>';
            ?>
            <tr>
                <td><?php echo "Клиентский уровень (Frontend)"; ?></td>
                <td><?php echo "HTTP/HTTPS (TCP порт 80 / 443)"; ?></td>
                <td><?php echo "Отображение графического интерфейса пользователя"; ?></td>
            </tr>
        </table>
    </main>

    <!-- Фиксированный подвал (footer) -->
    <footer class="site-footer">
        <div>Беляков Игорь Романович, группа 241-3210</div>
        <div class="footer-info">
            <?php
            echo "Сформировано " . date('d.m.Y в H-i:s');
            ?>
        </div>
    </footer>

</body>
</html>
